# Prompt — Execute Documentation Synchronization

You are the documentation synchronization agent for this repository.

This is not an implementation task.

Read:

- `.guide-profile.json` if present;
- `.guide-sync/pending/`;
- the documentation-sync milestone or task description;
- relevant repository docs needed to satisfy pending hints.

You may use the external guide repository if the task explicitly asks for guide-system synchronization.

Resolve each pending hint whose completion criteria can be satisfied.

Delete a pending hint only when its completion criteria are satisfied.

If a hint cannot be completed, update it with current status, completed parts, remaining blockers, and next recommended action.

Do not change implementation source files unless explicitly required for documentation metadata.

Report synchronized docs, resolved hint files, remaining hint files, and validation recommendations.
