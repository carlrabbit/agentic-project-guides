# Prompt — Plan a Broad AI-Executed Milestone

Create an implementation-ready package for:

```text
{milestone}
```

Execution mode: `ai-executed-broad`.

Use only with stable specs, architecture, acceptance criteria, and strong validation.

The milestone must declare validation execution mode. Broad or aggregate suites that may exceed agent command lifetimes must be resumable and expose a machine-readable plan plus fast verification.

Generate a filled execution prompt that instructs the implementation agent to execute shards separately and use the verifier as success authority.
