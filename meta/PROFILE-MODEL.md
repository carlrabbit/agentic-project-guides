# Profile Model

A profile is a reusable project category that activates documentation, engineering, validation, and release expectations.

Profiles avoid one universal repository template.

Example profiles:

```text
base
dotnet-library
source-generator
blazor-component
artifact-first-runtime
```

A profile defines applicability, documentation layers, engineering blocks, validation tiers, maturity behavior, repository-role behavior, and migration concerns.
